[workspace]
members = [
    "shared",
    "hydra",
    "moonshot",
    "grid",
    "trigon",
    "nexus",
    "candle-brain",

    "architect/cortex",
]
resolver = "3"

[profile.release]
opt-level = 3
lto = "thin"
codegen-units = 4
panic = "abort"
strip = true
debug = false
