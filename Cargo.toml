[workspace]
members = [
    "shared",
    "hydra",
    "moonshot",
    "grid",
    "trigon",
    "nexus",
    "candle-brain",

    "architect/cortex",
]
resolver = "3"

[profile.release]
opt-level = 3
lto = "fat"
codegen-units = 1
panic = "abort"
strip = true
debug = false
